import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#131516',
    },
    background: {
      default: '#191c1e',
    },
    text: {
      primary: '#fff'
    },
  },
});

export default theme;